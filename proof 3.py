#Defines two sets each with three elements
a = ['b', 'c', 'd']
e = ['f', 'g'. 'h']
#Creates a union of a, b within set a
for let in e:
    a += let
#Prints length of set a, equivalent to the union of a, b
print (len(a))
